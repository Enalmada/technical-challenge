import { NextPage } from "next";

// TODO - on page load, and perhaps timer check again
const MaintenanceMode: NextPage = () => {
    return <div>Maintenance Mode</div>;
};

export default MaintenanceMode;
